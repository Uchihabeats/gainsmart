version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    volumes:
      - app_data:/app/data
    restart: unless-stopped

volumes:
  app_data:
